# Copy outside source control, replace every value, then apply:
# kubectl apply -f secrets.local.yaml
apiVersion: v1
kind: Secret
metadata:
  name: paynexus-secrets
  namespace: paynexus
type: Opaque
stringData:
  DB_PASSWORD: SET_OUTSIDE_SOURCE_CONTROL
  POSTGRES_PASSWORD: SET_OUTSIDE_SOURCE_CONTROL
  KEYCLOAK_ADMIN: SET_OUTSIDE_SOURCE_CONTROL
  KEYCLOAK_ADMIN_PASSWORD: SET_OUTSIDE_SOURCE_CONTROL
  KEYCLOAK_PAYMENT_CLIENT_SECRET: SET_OUTSIDE_SOURCE_CONTROL
  GRAFANA_ADMIN_USER: SET_OUTSIDE_SOURCE_CONTROL
  GRAFANA_ADMIN_PASSWORD: SET_OUTSIDE_SOURCE_CONTROL
